/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['img.clerk.com', 'images.clerk.dev', '*.clerk.com', '*.clerk.dev','fakeimageupload.com']
    }
};

export default nextConfig;
