/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["clerk.com", "images.clerk.dev", "img.clerk.com","assets.adidas.com", "fakestoreapi.com"],
      },
};

export default nextConfig;
